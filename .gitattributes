* text=auto

# Shell scripts & git hooks must use LF (bash requires it)
*.sh text eol=lf
.githooks/** text eol=lf

# PowerShell scripts
*.ps1 text eol=crlf

# Vendored libraries / fonts are checked in as-is
*.min.js text eol=lf
*.woff2 binary
